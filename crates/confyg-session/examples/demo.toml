# the address we bind to — keep this comment attached to host
host = "0.0.0.0"

# deliberately out of range, so you can see an Invalid field
port = 99999

# a key the schema has never heard of
colour = "puce"

# a scalar array, two of a permitted four — Remove is gated at minItems
tags = ["edge", "beta"]

[[servers]]
host = "a.example"
weight = 2

[[servers]]
host = "b.example"

[[servers]]
host = "c.example"
