{
  "name": "confyg-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Web host for confyg (renders the Form IR over the confyg-ffi WASM boundary)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
